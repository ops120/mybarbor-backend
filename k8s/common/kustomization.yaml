# Kustomization for common resources
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: k8s-demo

resources:
  - namespace.yaml
  - secrets.yaml
  - pvc.yaml
  - ingress.yaml
  - hpa.yaml
