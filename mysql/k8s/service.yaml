# MySQL Service
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: k8s-demo
  labels:
    app: mysql
spec:
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
  selector:
    app: mysql
